(function () {
    'use strict';

    angular
        .module('app')
        .controller('HomeController', controller);

    /** @ngInject */
    function controller($timeout) {
        var vm = this;

        init();

        function init() {

        }

    }
})();
